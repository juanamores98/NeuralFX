# Validación automatizada de la consolidación

Código validado: [`bd2dfefc00c295a3bbf354d2ff4b5439ab3d5616`](https://github.com/juanamores98/NeuralFX/commit/bd2dfefc00c295a3bbf354d2ff4b5439ab3d5616).

Ejecución: [GitHub Actions 34299125908](https://github.com/juanamores98/NeuralFX/actions/runs/34299125908), finalizada correctamente el 9 de septiembre de 2026. Los commits posteriores de cierre solo añaden documentación.

| Comprobación | Resultado | Alcance |
|---|---|---|
| Hub WPF e IPC | 76 aprobadas, 0 fallos, 0 omitidas | Compilación y lógica de instalación, importación, confianza, rollback y memoria compartida |
| Mod completo con dobles | Compila en C# 7.3; 1.149 comprobaciones | Incluye 1.000 iteraciones de cámara desconectada y 128 combinaciones de capacidades; no son 1.149 escenarios independientes |
| Decoder real del bridge | ASan/UBSan y MSVC aprobados | V1/V2/V3, tamaños, NaN/Inf, replay, epochs, Off, controles y errores persistentes |
| D3D11 WARP | Aprobado | Descriptor textura/SRV/escala, fallback, 100/85/66, extents impares y retirada diferida de recursos |
| Addon completo y smoke host | Compilados con MSVC | Upstream fijado, parches exactos, headers y enlace del addon |
| Scripts PowerShell | Parseados correctamente | Build, smoke, paquete y recogida de evidencia |

Comandos ejecutados por CI:

```text
g++ -std=c++17 -g -O1 -fsanitize=address,undefined -fno-omit-frame-pointer Native/bridge_tests.cpp -o /tmp/bridge-tests
/tmp/bridge-tests
dotnet test NeuralFX.Tests/NeuralFX.Tests.csproj -c Release -p:BuildMod=false
dotnet run --project tests/ModContracts/ModContracts.csproj -c Release
./Native/build.ps1 -SmokeHarness
```

`BuildMod=false` permite probar el Hub sin los ensamblados propietarios del juego. El proyecto de dobles enlaza todas las fuentes del mod y del protocolo; no produce una DLL instalable en CS1. El artefacto de CI incluye el addon, host y manifiesto generados, además del resultado TRX.

No se ejecutó CS1, NVIDIA NR, NGX en GPU NVIDIA ni la navegación real del Hub. WARP verifica el descriptor con patrones conocidos y su SRV, no la entrada final de NGX en una ciudad. La compilación con los ensamblados reales, la imagen y la estabilidad quedan pendientes. Pre-UI, confirmación NR por frame, jitter preparado y SR interno aún requieren implementación, no solo repetir estas pruebas.

Las ejecuciones intermedias detectaron expectativas antiguas de Performance Mode, un conflicto de nombre local en el manager y diferencias CRLF/LF en el parcheador de Windows. Fueron corregidos; la ejecución enlazada arriba incluye esos cambios.

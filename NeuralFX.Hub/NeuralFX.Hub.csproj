<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>NeuralFX.Hub</RootNamespace>
    <AssemblyName>NeuralFX.Hub</AssemblyName>
    <Version>2.1.1</Version>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="..\Native\out\capabilities.json" LogicalName="NeuralFX.Native.Capabilities" Condition="Exists('..\Native\out\capabilities.json')" />
    <EmbeddedResource Include="..\Native\out\dlss5-feed.addon64" LogicalName="NeuralFX.Native.Feeder" Condition="Exists('..\Native\out\dlss5-feed.addon64')" />
    <EmbeddedResource Include="..\Native\out\DLSS5_Feed.fx" LogicalName="NeuralFX.Native.Shader" Condition="Exists('..\Native\out\DLSS5_Feed.fx')" />
    <EmbeddedResource Include="..\NeuralFX.Mod\bin\Release\net35\NeuralFX.dll" LogicalName="NeuralFX.Mod.Assembly" Condition="Exists('..\NeuralFX.Mod\bin\Release\net35\NeuralFX.dll')" />
    <EmbeddedResource Include="Assets\**\*" />
    <Compile Include="..\Shared\*.cs" Link="Protocol\%(Filename)%(Extension)" />
  </ItemGroup>

  <ItemGroup>
    <!-- Compilar el Hub (o las pruebas) construye el mod para incrustarlo, pero no lo despliega:
         el despliegue en caliente pertenece a quien compila NeuralFX.Mod. -->
    <ProjectReference Include="..\NeuralFX.Mod\NeuralFX.Mod.csproj" ReferenceOutputAssembly="false" SkipGetTargetFrameworkProperties="true" AdditionalProperties="DeployMod=false" Condition="'$(BuildMod)' != 'false'" />
  </ItemGroup>

  <Target Name="RequireNativeBridgeForPublish" BeforeTargets="Publish">
    <Error Condition="!Exists('..\Native\out\dlss5-feed.addon64')" Text="Run Native/build.ps1 before publishing the Hub." />
    <Error Condition="!Exists('..\Native\out\DLSS5_Feed.fx')" Text="Run Native/build.ps1 to generate the companion shader before publishing." />
  </Target>

</Project>
